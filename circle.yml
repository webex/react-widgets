machine:
  environment:
    COVERAGE: true
    NODE_ENV: test
    XUNIT: true
    UNIT_ONLY: true
  node:
    version: 8.9.1

general:
  artifacts:
    - npm-debug.log

dependencies:
  cache_directories:
    - node_modules

test:
  override:
    - npm run static-analysis
    - npm run jest -- --runInBand
