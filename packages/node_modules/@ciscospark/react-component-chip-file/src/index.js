import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';
import Icon, {ICON_TYPE_DOCUMENT} from '@ciscospark/react-component-icon';
import ChipBase from '@ciscospark/react-component-chip-base';

import styles from './styles.css';

const propTypes = {
  name: PropTypes.string.isRequired,
  size: PropTypes.string,
  thumbnail: PropTypes.string,
  type: PropTypes.string
};

const defaultProps = {
  size: '',
  thumbnail: '',
  type: ''
};


function ChipFile(props) {
  const {
    name,
    size,
    thumbnail,
    type
  } = props;
  let icon;
  if (!thumbnail) {
    icon = (
      <div className={classNames('ciscospark-file-icon', styles.icon)}>
        <Icon type={ICON_TYPE_DOCUMENT} />
      </div>
    );
  }

  return (
    <ChipBase {...props}>
      <div className={classNames('ciscospark-file-thumbnail', styles.thumbnail)}>
        {icon}
        <img alt="" src={thumbnail} />
      </div>
      <div className={classNames('ciscospark-file-info', styles.info)}>
        <div className={classNames('ciscospark-file-name', styles.name)}>{name}</div>
        <div className={classNames('ciscospark-file-meta', styles.meta)}>
          <div className={classNames('ciscospark-file-size', styles.size)}>{size}</div>
          <div className={classNames('ciscospark-file-type', styles.type)}>{type}</div>
        </div>
      </div>
    </ChipBase>
  );
}

ChipFile.propTypes = propTypes;
ChipFile.defaultProps = defaultProps;

export default ChipFile;
