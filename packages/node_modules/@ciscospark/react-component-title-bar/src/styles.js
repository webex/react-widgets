import {StyleSheet} from 'aphrodite/no-important';

const styles = StyleSheet.create({
  titleBar: {
    display: `flex`,
    height: `60px`,
    fontFamily: `'CiscoSans TT Extra Light', 'Helvetica Neue', Arial`,
    borderBottom: `1px solid #ececed`
  }
});

export default styles;
