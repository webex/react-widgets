import React from 'react';
import renderer from 'react-test-renderer';

import ActivityMenu from '.';

describe('ActivityMenu component', () => {
  const onExit = jest.fn();
  const buttons = [
    {
      label: 'Message',
      onClick: jest.fn()
    },
    {
      label: 'Call',
      onClick: jest.fn()
    }
  ];
  const component = renderer.create(<ActivityMenu
    buttons={buttons}
    onExit={onExit}
    showExitButton
  />);

  it('renders properly', () => {
    expect(component).toMatchSnapshot();
  });
});
