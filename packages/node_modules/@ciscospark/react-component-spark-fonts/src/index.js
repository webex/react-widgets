import './fonts.css';

import React, {PropTypes} from 'react';

const SparkFonts = (props) =>
  <div>
    {props.children}
  </div>;

SparkFonts.propTypes = {
  children: PropTypes.node
};

export default SparkFonts;
