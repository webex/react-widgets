import React, {PropTypes} from 'react';
import classNames from 'classnames';
import styles from './styles.css';

export function Cover({message}) {
  return (
    <div className={classNames(`cover-message`, styles.message)}>
      <div className={classNames(`cover-message-title`, styles.title)}>
        {message}
      </div>
    </div>
  );
}

Cover.propTypes = {
  message: PropTypes.string
};
