import React from 'react';
import PropTypes from 'prop-types';

import classNames from 'classnames';

import styles from './styles.css';

export default function spinner({bright}) {
  return <div className={classNames('ciscospark-spinner', styles.spinner, {[styles.bright]: bright})} />;
}

spinner.propTypes = {
  bright: PropTypes.bool
};
