import React from 'react';
import renderer from 'react-test-renderer';

import Badge from '.';

function createBadge({children}) {
  return renderer.create(
    <Badge>{children}</Badge>
  );
}

describe(`Badge component`, () => {
  let props;

  beforeEach(() => {
    props = {
      children: `+100`
    };
  });

  it(`renders properly`, () => {
    expect(createBadge(props)).toMatchSnapshot();
  });
});
