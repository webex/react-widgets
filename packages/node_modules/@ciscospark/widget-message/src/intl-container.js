import React, {PropTypes} from 'react';
import {addLocaleData, IntlProvider} from 'react-intl';
import enLocaleData from 'react-intl/locale-data/en';

import Message from './container';
import enTranslationMessages from './translations/en';


export default function MessageWidgetWithIntl({spark, toPersonEmail, toPersonId, user, onEvent}) {
  addLocaleData(enLocaleData);
  return (
    <IntlProvider locale="en" messages={enTranslationMessages}>
      <Message
        onEvent={onEvent}
        spark={spark}
        toPersonEmail={toPersonEmail}
        toPersonId={toPersonId}
        user={user}
      />
    </IntlProvider>
  );
}

MessageWidgetWithIntl.propTypes = {
  onEvent: PropTypes.func,
  spark: PropTypes.object,
  toPersonEmail: PropTypes.string,
  toPersonId: PropTypes.string,
  user: PropTypes.object
};
