import constructSparkWidget from '@ciscospark/widget-base';

import ConnectedMessage, {ownPropTypes} from './container';
import enTranslationMessages from './translations/en';

export {
  MessageWidget as MessageComponent
} from './container';

export * from './actions';
import {reducer, reducers} from './reducer';

export {reducer, reducers};

export default constructSparkWidget(
  'message',
  ConnectedMessage,
  {
    reducers,
    intl: {
      messages: enTranslationMessages
    },
    propTypes: ownPropTypes
  }
);
