export function constructActivity(activity) {
  return {
    id: activity.id,
    actor: activity.actor ? activity.actor.id : null,
    type: activity.verb,
    object: activity.object
  };
}

export function constructActivities(activities) {
  return activities.map(constructActivity);
}

